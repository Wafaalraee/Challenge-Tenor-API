import React from 'react';
import ReactDOM from 'react-dom';
import TenorSearchApp from './App';  // Adjust the import path accordingly

ReactDOM.render(
  <React.StrictMode>
    <TenorSearchApp />
  </React.StrictMode>,
  document.getElementById('root')
);

